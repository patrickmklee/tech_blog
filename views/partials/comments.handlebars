{{#each this}}

<div class="media border-top border-bottom border-dark">
<div class="media-body py-2">
    <h6 class="fs-6 mb-3">
    {{user.username}} on {{format_date created_at}}
    </h6>
    <h5 class="mt-0">{{comment_text}}</h5>
</div>
</div>
{{/each}}
